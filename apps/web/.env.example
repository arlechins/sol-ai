# Point the read-only demo at your own RPC (public devnet is the fallback).
# If you change the host, add it to connect-src in apps/web/public/_headers.
# VITE_SOLANA_RPC_URL=https://api.devnet.solana.com

# Expose the variant switcher in production builds.
# VITE_SHOW_VARIANTS=1
